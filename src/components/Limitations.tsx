import { Section } from './layout/Section';
export function Limitations() {
  return <Section id="limitations" eyebrow="Interpretation" title="Interpret performance within the declared experiment"><div className="card space-y-3 p-5 text-[12px] leading-relaxed text-ink-500"><p>Results describe the named checkpoint, samples, relation and removal protocol. They do not establish real-world causality or a statistically significant accuracy gain.</p><p>Deletion may change normalization and later computations. Different models need not have the same node meaning or intervention effect.</p><p>The 0.1% relative display threshold describes small changes. Missing experiments remain unavailable.</p><p>Result validation checks structure and available metric arithmetic. Correct native edge removal requires separate model-specific verification.</p></div></Section>;
}
