# Third-party notice: Microsoft StemGNN

The StemGNN source used by this example is available from
<https://github.com/microsoft/StemGNN> at revision
`dc7dea6842c20c5dcece2f18b435e26770b3421c`.

DGraInsight does not redistribute Microsoft StemGNN source or a modified copy of
`models/base_model.py`. At the pinned revision, the upstream repository contains
no `LICENSE`, `LICENCE`, `COPYING`, or `NOTICE` file, and its README contains no
license statement (verified 2026-09-19). Reviewers obtain that source directly
from the upstream repository and are responsible for any terms supplied by the
upstream project or rightsholder.

The adapter first verifies the reviewer-supplied `models/base_model.py` against
SHA-256
`fa247c2036046091c6f68e27967ce45ace3e04ed16bbf2e957cc8df56374500a`.
For current PyTorch, it then constructs an in-memory compatibility copy by
mechanically replacing the removed `torch.rfft` and `torch.irfft` calls with the
corresponding `torch.fft.fft` and `torch.fft.ifft` APIs. The generated bytes are
verified against SHA-256
`ae87de6c4ed4c011ec35bb28bcea194b291f9b14ed3ae306ef19faac38eb9f25`,
executed in memory, and neither written nor included in the release package.

The DGraInsight MIT License does not grant rights to Microsoft StemGNN or any
other third-party code.
